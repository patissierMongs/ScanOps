# QA-045 — --udp single-profile flag (-sU insertion in build_base_flags) has no test

- **group**: R3   **disposition**: FILE-ONLY (low-value coverage; no code defect)   **severity**: low   **dimension**: coverage-gaps
- **dedup_key**: `udp-single-profile-flag-untested`
- **location**: scanner/scanops_scanner.py build_base_flags (`elif args.udp and "-sU" not in flags: flags.insert(1 if flags[0] in SCAN_TYPE_FLAGS else 0, "-sU")`)

## Evidence
No test passes `udp=True` to build_base_flags; the only 'udp' token in tests is `--udp-all-targets` (a different argument). The position-aware insert (index 1 after a leading scan-type flag vs index 0) is real and reachable but unasserted. The production code is correct — this is a pure missing-test gap over trivial, already-correct logic.

## Proposed fix
Optional cheap test: `build_base_flags(udp=True)` asserts `-sU` present; on a scan-type-leading profile (quick→`-sT`) `-sU` is inserted at index 1; `--udp` with `--tcp-only` does not add `-sU`.

## Disposition note
The adversarial verifier rated this overengineering=true (no defect; a bad ordering would surface at runtime). Recorded as FILE-ONLY. A 3-line lock test may still be added opportunistically since the project explicitly values coverage, but no code change is warranted.
