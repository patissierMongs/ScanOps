# ScanOps backend dependencies (kept minimal for air-gapped install).
# Offline install: pip install --no-index --find-links packaging/wheelhouse -r requirements.txt
fastapi==0.115.6
uvicorn==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
openpyxl==3.1.5
