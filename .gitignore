# Python
backend/.venv/
__pycache__/
*.pyc

# 운영/런타임 데이터 — 절대 커밋 금지 (DB·관리자 비밀번호·시크릿키·스캔결과)
backend/data/
/data/
/demo_data/
*.db
*.db-shm
*.db-wal
INITIAL_ADMIN.txt
secret.key
samples/.token

# Frontend
frontend/node_modules/
# frontend/dist 는 에어갭 배포용으로 커밋(빌드 산출물 동봉)

# E2E(Playwright)·테스트/린트 산출물 — 재생성되므로 커밋 안 함
frontend/test-results/
frontend/playwright-report/
frontend/.e2e-data/
frontend/coverage/
backend/.e2e-data/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# 패키징 wheelhouse 는 에어갭 배포용으로 포함(cp312 + 순수파이썬만 — 임베디드 3.12 와 일치)

# 데모 스크린샷은 추적 안 함(레포 비대) — 필요하면 mockups/ 나 위키로
samples/*.png

# 데모/검증 스크립트의 런타임 산출물 — 스크립트로 재생성되므로 커밋 안 함
# (스크립트 .py/.mjs/.sh 와 큐레이션된 샘플 XML·스크린샷은 그대로 추적)
live_sample/demo_out.txt
live_sample/sample1_*.json
live_sample/scan_live.xml
live_sample/verify*.json

# 스캔 엔진/랩 런타임 산출물 — 스캔으로 재생성(XML·이벤트·상태). 하니스 코드는 추적.
engine/engine-out/
lab/samples/
# Standalone scanner outputs
scanops_scans/
scanner/scanops_scans/

# Claude Code 로컬 설정(머신별)
.claude/settings.local.json
