// 한 페이지에 몇 건을 볼지. 목록마다 고정 값이면 '다음' 을 수십 번 눌러야 전체를 볼 수
// 있고, 반대로 언제나 전부 그리면 수천 행에서 브라우저가 멈춘다. 선택은 사람이 한다.
export const PAGE_SIZES = [100, 200, 500, 1000, 5000];
export const DEFAULT_PAGE_SIZE = 200;
